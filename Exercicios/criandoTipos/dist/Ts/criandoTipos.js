let literal; //Esse é um tipo literal, no caso ele é literalmente esse valor então n posso alterar(sempre vai ser essa frase q coloquei)
let option; //Esse tipo diz q essa variavel pode ter apenas o valor 1 ou o valor 2, m,as poderia ser tipos, ex: string | number
let computer; //Estou dizendo q essa variável é do tipo pc
function teste(x) {
    //teste
}
function n(x) {
    const firstname = "Mamute";
    x(firstname);
}
